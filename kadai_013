def add_calculate_tax_price(price, tax_rate):
  tax = price * tax_rate * 0.01
  total = price + tax
  return total

price1 = 100
price2 = 200

tax = 1.1
def total():

  return price1 + price2

print (total() * tax)
